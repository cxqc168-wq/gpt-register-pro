# GPT Register Pro 使用说明

本文按一台新机器从零部署的顺序编写。所有示例里的密钥、域名、Worker 地址都要替换成你自己的值。

## 目录

1. [项目简介](#1-项目简介)
2. [需要先准备什么](#2-需要先准备什么)
3. [安装与运行](#3-安装与运行)
4. [接码平台配置（三选一）](#4-接码平台配置三选一)
5. [临时邮箱配置](#5-临时邮箱配置)
6. [项目配置文件](#6-项目配置文件)
7. [桌面控制台使用](#7-桌面控制台使用)
8. [命令行使用](#8-命令行使用)
9. [注册流程说明](#9-注册流程说明)
10. [产物说明](#10-产物说明)
11. [分阶段恢复](#11-分阶段恢复)
12. [Outlook 邮箱池模式](#12-outlook-邮箱池模式)
13. [导入 sub2api](#13-导入-sub2api)
14. [常见问题](#14-常见问题)
15. [不要提交的文件](#15-不要提交的文件)

---

## 1. 项目简介

GPT Register Pro 是一个 ChatGPT / Codex 账号自动化注册工具，支持：

- **三大接码平台**：HeroSMS、Grizzly SMS、NexSMS，可在配置中切换
- **多种邮箱后端**：Cloudflare Worker 临时邮箱、cloud-mail、Outlook 真实邮箱池
- **浏览器自动化**：基于 puppeteer-real-browser，自动绕过 Cloudflare Turnstile
- **OAuth 换 Token**：通过 OpenAI OAuth PKCE 流程换取 Codex Session Token
- **桌面控制台**：Electron 图形界面，可视化配置、运行、监控
- **并发注册**：支持多 Worker 并发批量注册
- **分阶段恢复**：支持从任意阶段断点续跑

## 2. 需要先准备什么

1. **一个接码平台 API Key**（三选一）：
   - HeroSMS：[注册链接](https://hero-sms.com/)
   - Grizzly SMS：[注册链接](https://grizzlysms.com/cn/?r=1743955)
   - NexSMS：[注册链接](https://user.nexsms.net/#/auth/sign-up?ref=qdMkySge)
2. **一个临时邮箱方案**（二选一）：
   - Cloudflare 域名 + Worker + D1（推荐，零成本）
   - Outlook 邮箱池（需要提前准备卡密）
3. **一个可用代理**，建议本机 HTTP 代理，例如 `127.0.0.1:7897`。没有代理也能配置为空，但 ChatGPT 页面可能打不开。
4. **一台能打开图形浏览器的机器**（Windows / macOS / Linux 桌面环境）。
5. **Node.js 18 或更高版本**（命令行模式需要；桌面版已内置运行时）。
6. **Google Chrome 或 Chromium**。

检查 Node 版本：

```bash
node -v
npm -v
```

## 3. 安装与运行

### 3.1 桌面版（推荐）

从 [GitHub Releases](https://github.com/your-org/gpt-register-pro/releases) 下载安装包：

- `GPT Register Pro-1.0.0-x64.exe` — NSIS 安装包（可选择安装目录）
- `GPT Register Pro-1.0.0-x64.msi` — MSI 安装包
- `GPT Register Pro-1.0.0-x64.exe`（portable）— 免安装便携版

安装后启动，在「后台配置」页面填写接码 API Key 和邮箱配置即可开始使用。

### 3.2 源码运行

```bash
git clone https://github.com/your-org/gpt-register-pro.git
cd gpt-register-pro
npm install
```

启动桌面控制台：

```bash
npm start
```

或直接运行命令行：

```bash
node index.js 1
```

### 3.3 确认 Chrome 路径

Windows 常见路径：

```text
C:\Program Files\Google\Chrome\Application\chrome.exe
```

macOS 常见路径：

```text
/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
```

Linux 常见路径：

```text
/usr/bin/google-chrome-stable
```

## 4. 接码平台配置（三选一）

本项目支持三家接码平台，在 `config.json` 中通过 `smsProvider` 字段切换。

### 4.1 HeroSMS

**注册链接**：[https://hero-sms.com/](https://hero-sms.com/)

> 促销码 `plucksu`（有效期至 2026-12-31）：注册后使用可获得会员等级 2 级，号码价格降低 15%。

配置示例：

```json
{
  "smsProvider": "herosms",
  "heroSmsApiKey": "YOUR_HEROSMS_API_KEY",
  "heroSmsService": "dr",
  "heroSmsCountry": 46,
  "phoneCountryCode": "SE",
  "heroSmsPromptCountrySelection": false
}
```

说明：
- `heroSmsService: "dr"` 对应 OpenAI / ChatGPT 服务代码
- `heroSmsCountry: 46` 是 HeroSMS 的瑞典国家 ID
- `phoneCountryCode: "SE"` 是网页国家选择用的 ISO 代码
- `heroSmsPromptCountrySelection: false` 表示启动时不手动选国家
- 设为 `true` 时，启动时会按价格从低到高展示可选国家列表

### 4.2 Grizzly SMS

**注册链接**：[https://grizzlysms.com/cn/?r=1743955](https://grizzlysms.com/cn/?r=1743955)

Grizzly SMS 协议与 sms-activate 兼容，支持中文界面。

配置示例：

```json
{
  "smsProvider": "grizzly",
  "grizzlySmsApiKey": "YOUR_GRIZZLY_API_KEY",
  "grizzlySmsService": "dr",
  "heroSmsCountry": 16,
  "phoneCountryCode": "GB",
  "heroSmsPromptCountrySelection": true
}
```

特点：
- 验证码默认等待 3 分钟（比 HeroSMS 更长）
- 取消激活时会轮询重试，确保号码退款成功
- 国家列表优先返回中文名称

### 4.3 NexSMS

**注册链接**：[https://user.nexsms.net/#/auth/sign-up?ref=qdMkySge](https://user.nexsms.net/#/auth/sign-up?ref=qdMkySge)

NexSMS 使用 REST API（非 sms-activate 协议），以手机号为核心生命周期。

配置示例：

```json
{
  "smsProvider": "nexsms",
  "nexSmsApiKey": "YOUR_NEXSMS_API_KEY",
  "nexSmsService": "dr",
  "heroSmsCountry": 16,
  "phoneCountryCode": "GB",
  "heroSmsPromptCountrySelection": true
}
```

特点：
- 无 activationId / setStatus 概念，购买后直接轮询短信
- 价格接口一次返回全部国家报价（含最低价、中位数、最高价）
- 取消激活需号码创建超 2 分钟，失败不阻塞主流程

### 4.4 三家平台对比

| 特性 | HeroSMS | Grizzly SMS | NexSMS |
| --- | --- | --- | --- |
| 协议 | sms-activate | sms-activate 兼容 | REST API |
| 服务代码 | `dr` | `dr` | `dr` |
| 国家选择 | 支持按价格排序 | 支持按价格排序 | 支持按价格排序 |
| 验证码等待 | 默认 5 分钟 | 默认 3 分钟 | 默认 5 分钟 |
| 取消退款 | 立即取消 | 轮询确保成功 | 2 分钟后可取消 |
| 余额查询 | `ACCESS_BALANCE` 文本 | `ACCESS_BALANCE` 文本 | JSON 响应 |
| API Key 字段 | `heroSmsApiKey` | `grizzlySmsApiKey` | `nexSmsApiKey` |

## 5. 临时邮箱配置

### 方案 A：Cloudflare Worker 临时邮箱（推荐）

这一步用于接收 OpenAI 邮箱验证码，零成本且完全自控。

#### 5.1 域名接入 Cloudflare

1. 登录 Cloudflare。
2. 添加你的域名，例如 `yourdomain.com`。
3. 按 Cloudflare 提示，把域名注册商里的 NS 服务器改成 Cloudflare 给出的 NS。
4. 等待 Cloudflare 显示域名为 Active。

#### 5.2 创建 D1 数据库

1. 进入 Cloudflare 后台 → `Workers & Pages` → `D1 SQL Database`。
2. 点击创建数据库，名字填 `mail_db`。
3. 进入数据库 → 打开 Console。
4. 把项目里的 `cloudflare-email-worker-schema.sql` 内容粘进去执行。

SQL 内容：

```sql
CREATE TABLE IF NOT EXISTS mailboxes (
  address TEXT PRIMARY KEY,
  created_at INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS mails (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  address TEXT NOT NULL,
  sender TEXT,
  subject TEXT,
  raw TEXT NOT NULL,
  received_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_mails_address_received
  ON mails (address, received_at DESC);
```

#### 5.3 创建 Worker

1. 进入 `Workers & Pages` → 创建 Worker。
2. Worker 名字取 `mail-worker`。
3. 打开 Worker 编辑器，删除默认代码。
4. 把项目里的 `cloudflare-email-worker.js` 全部粘进去。
5. 保存并部署。

部署后得到地址，例如：

```text
https://mail-worker.your-name.workers.dev
```

#### 5.4 绑定 D1 和环境变量

进入 Worker → Settings：

1. Bindings → 添加 D1 database binding，Variable name 必须填 `DB`，选择 `mail_db`。
2. Variables → 添加：
   - `MAIL_API_TOKEN` = 自己设置一个查询密钥（如随机字符串）
   - `MAIL_DOMAIN` = `yourdomain.com`

#### 5.5 开启 Email Routing

1. 回到 Cloudflare 域名页面 → `Email` / `Email Routing`。
2. 按提示启用 Email Routing，添加 MX/TXT 记录。
3. 进入 Routing rules → Catch-all 地址 → 选择 `Send to Worker` → 选择 `mail-worker`。
4. 保存。

#### 5.6 测试 Worker

```bash
curl -H "Authorization: Bearer CHANGE_ME_RANDOM_TOKEN" \
  https://mail-worker.your-name.workers.dev/api/health
```

成功返回 `{"ok":true}`。

创建邮箱测试：

```bash
curl -X POST \
  -H "Authorization: Bearer CHANGE_ME_RANDOM_TOKEN" \
  -H "content-type: application/json" \
  -d '{"name":"test123","domain":"yourdomain.com"}' \
  https://mail-worker.your-name.workers.dev/api/create-address
```

#### 5.7 Worker API 一览

所有请求需要 `Authorization: Bearer MAIL_API_TOKEN` 头。

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/health` | GET | 健康检查 |
| `/api/create-address` | POST | 创建邮箱 |
| `/api/mails?address=&limit=&offset=` | GET | 按地址查询邮件 |

### 方案 B：Outlook 真实邮箱池

如果你有 Outlook 卡密（格式：`邮箱----密码----client_id----refresh_token`），可以使用真实邮箱模式。

1. 在桌面控制台「Outlook 邮箱池」页面导入卡密。
2. 配置中设置 `"mailProvider": "outlook"`。
3. 系统会自动从池中分配邮箱，通过 Microsoft Graph API 或 IMAP XOAUTH2 收取验证码。

详见 [第 12 节：Outlook 邮箱池模式](#12-outlook-邮箱池模式)。

## 6. 项目配置文件

### 6.1 配置文件层级

项目读取基础配置 + 环境覆盖配置：

| 文件 | 说明 |
| --- | --- |
| `config.json` | 基础配置（桌面控制台保存到此） |
| `config.local.json` | macOS 默认覆盖配置 |
| `config.server.json` | Linux 默认覆盖配置 |
| `config.example.json` | 示例配置模板 |

也可以手动指定：

```bash
CONFIG_FILE=./config.server.json node index.js 1
CONFIG_PROFILE=server node index.js 1
```

### 6.2 完整配置示例

```json
{
  "smsProvider": "herosms",
  "heroSmsApiKey": "YOUR_HEROSMS_API_KEY",
  "heroSmsService": "dr",
  "nexSmsApiKey": "",
  "nexSmsService": "dr",
  "grizzlySmsApiKey": "",
  "grizzlySmsService": "dr",
  "heroSmsCountry": 16,
  "heroSmsPromptCountrySelection": true,
  "heroSmsCountryTopN": 10,
  "phoneCountryCode": "GB",

  "mailProvider": "cloudflare-worker",
  "mailBaseUrl": "https://mail-worker.your-name.workers.dev",
  "mailAdminToken": "YOUR_MAIL_API_TOKEN",
  "mailAdminPassword": "",
  "mailSitePassword": "",
  "mailAdminEmail": "",
  "mailDomain": "yourdomain.com",
  "mailDomains": ["yourdomain.com"],

  "outlookPoolFile": "outlook-accounts.json",
  "outlookUseProxy": true,

  "proxyHost": "127.0.0.1",
  "proxyPort": 7897,
  "proxyUsername": "",
  "proxyPassword": "",

  "useChrome": true,
  "chromePath": "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe",
  "browserUserDataDir": "browser-profile",
  "browserIncognito": false,
  "browserClearChatGptSession": true,

  "targetTokenCount": 1,
  "tokenOutputDir": "tokens",
  "tokenOutputDirs": ["tokens"]
}
```

### 6.3 配置字段说明

| 字段 | 说明 |
| --- | --- |
| `smsProvider` | 接码平台：`herosms` / `grizzly` / `nexsms` |
| `heroSmsApiKey` | HeroSMS API Key |
| `grizzlySmsApiKey` | Grizzly SMS API Key |
| `nexSmsApiKey` | NexSMS API Key |
| `heroSmsService` | 服务代码，OpenAI = `dr` |
| `heroSmsCountry` | 默认国家 ID（英国=16，瑞典=46，美国=187） |
| `phoneCountryCode` | 手机国家 ISO 代码（GB/SE/US 等） |
| `heroSmsPromptCountrySelection` | 启动时是否交互选择低价国家 |
| `mailProvider` | 邮箱类型：`cloudflare-worker` / `cloud-mail` / `legacy` / `outlook` |
| `mailBaseUrl` | 邮箱服务地址 |
| `mailAdminToken` | 邮箱接口 Token |
| `mailDomain` / `mailDomains` | 邮箱域名 |
| `proxyHost` / `proxyPort` | 代理地址 |
| `chromePath` | Chrome 可执行文件路径 |
| `browserUserDataDir` | 浏览器持久化 profile 目录 |
| `browserClearChatGptSession` | 每次注册前清理 ChatGPT 登录态 |
| `tokenOutputDirs` | Token 输出目录（支持多目录） |

代理也可通过 `HTTP_PROXY`、`HTTPS_PROXY` 或 `ALL_PROXY` 环境变量提供。

## 7. 桌面控制台使用

桌面控制台包含以下页面：

### 7.1 控制台（Console）

- 显示账号数、邮箱绑定数、Token 数统计
- 配置检查提示（缺失项会红色标注）
- 运行控制：选择模式、目标数量、并发数，点击开始
- 实时日志输出

运行模式：
- **完整流程**：手机号注册 → 邮箱绑定 → OAuth 换 Token
- **仅手机号**：只注册手机号账号，不绑定邮箱
- **纯邮箱注册**：使用 Outlook 邮箱池直接注册（无需接码）
- **仅绑定邮箱**（Phase 2）：从已有手机号账号继续绑定邮箱
- **仅换 Token**（Phase 3）：从已绑定邮箱的账号换取 Token
- **批量补 Token**（Phase 8）：按 `username.json` 批量换 Token

### 7.2 后台配置（Settings）

可视化编辑 `config.json`，包括接码平台切换、API Key、邮箱配置、代理、浏览器路径等。保存后立即生效。

### 7.3 接码配置（SMS Config）

- 显示当前接码平台余额
- 展示各国家号码价格和库存（按价格排序）
- 可直接选择目标国家

### 7.4 Outlook 邮箱池

- 导入卡密（支持文本粘贴或文件选择）
- 查看账号列表和状态（可用/使用中/已用/无效）
- 单个账号连通性测试
- 查看邮箱最近邮件
- 重置账号状态

### 7.5 账号信息（Accounts）

合并展示 `accounts.json`、`username.json`、Outlook 池和 Token 文件的账号信息，包括手机号、邮箱、注册时间、是否有 Token 等。

### 7.6 Token 状态（Token Status）

- 扫描所有 Token 文件
- 自动刷新已过期的 access_token（通过 refresh_token）
- 显示每个 Token 的邮箱、状态、过期时间

## 8. 命令行使用

完整流程，注册 1 个账号：

```bash
node index.js 1
```

批量注册 5 个：

```bash
node index.js 5
```

指定国家：

```bash
node index.js 1 --country=SE
```

并发注册（默认 3 并发）：

```bash
node index.js 5 --concurrency=5
```

新注册并停在 Phase 2 收尾：

```bash
node index.js 1 --stop-after-phase2
```

使用已注册账号继续绑定邮箱：

```bash
node index.js --phase2
```

只补最后一步 Token：

```bash
node index.js --phase3
```

按 `username.json` 批量补 Token：

```bash
node index.js --phase8
```

纯邮箱注册（Outlook 池模式）：

```bash
node index.js --email
```

仅手机号注册（不绑定邮箱）：

```bash
node index.js --phone-only 1
```

## 9. 注册流程说明

默认完整流程分为四个阶段：

| 阶段 | 说明 |
| --- | --- |
| Phase 1 | 用接码平台获取手机号，在 ChatGPT 注册页面完成短信验证 |
| Phase 1.5 | 首次进入 ChatGPT，补全 about-you 信息 |
| Phase 2 | 进入 Codex OAuth，给手机号账号绑定临时邮箱 |
| Phase 3 | 用邮箱登录 Codex OAuth，换取并保存 Token |

成功后输出：

```text
[OAuth] Token 成功保存至: tokens/codex-xxx@yourdomain.com-free.json
[完成] Token 数量 (1) 已达目标 (1)。
```

## 10. 产物说明

| 路径 | 说明 |
| --- | --- |
| `accounts.json` | 手机号注册账号池 |
| `username.json` | 已绑定邮箱的账号池 |
| `tokens/codex-<email>-free.json` | 生成的 Codex Session Token |
| `shibai.json` | 批量补 Token 失败记录 |
| `outlook-accounts.json` | Outlook 邮箱池数据 |
| `token-status.json` | Token 状态扫描缓存 |
| `logs/` | 运行日志 |
| `browser-profile/` | 浏览器持久化 profile |

## 11. 分阶段恢复

### 11.1 只继续绑定邮箱

如果手机号注册成功但邮箱绑定失败：

```bash
node index.js --phase2
```

脚本会从 `accounts.json` 列出账号，选择一个继续绑定邮箱。

### 11.2 只补 Token

如果邮箱绑定已成功但最后没拿到 Token：

```bash
node index.js --phase3
```

### 11.3 批量补 Token

```bash
node index.js --phase8
```

按 `username.json` 记录批量换 Token，失败写入 `shibai.json`。

## 12. Outlook 邮箱池模式

### 12.1 卡密格式

每行一个账号，用 `----` 分隔：

```text
邮箱----密码----client_id----refresh_token
```

示例：

```text
example@outlook.com----password123----abc123def456----eyJ0eXAiOi...（真实refresh_token，通常800字符以上）
```

- `#` 开头为注释，空行跳过
- refresh_token 必须是真实凭据（通常 800 字符以上），占位符会被拒绝

### 12.2 账号状态

| 状态 | 说明 |
| --- | --- |
| available | 可用，等待分配 |
| pending | 已分配，正在使用中 |
| used | 已绑定成功，消耗 |
| invalid | Token 失效或邮箱烧毁，需重新导入卡密 |

### 12.3 取件方式

系统自动探测账号可用的 OAuth scope：
1. 优先使用 **Microsoft Graph API** 收件
2. 失败回退到 **IMAP XOAUTH2** 收件
3. 同时拉取收件箱和垃圾箱

### 12.4 Token 轮换

OAuth 刷新时如果返回新的 refresh_token，会自动回写到账号池，避免旧 Token 失效。

## 13. 导入 sub2api

sub2api 支持 OpenAI Codex session 导入。

单个 Token：

1. 打开 sub2api 后台 → Accounts → 新建账号
2. 平台选择 OpenAI，类型选择 OAuth / ChatGPT OAuth
3. 选择 Codex Session Import
4. 打开 `tokens/codex-邮箱-free.json`，把整个 JSON 内容粘贴进去
5. 保存

批量导入：把多个 Token 文件整理成 JSONL，每行一个完整 JSON：

```jsonl
{"access_token":"...","refresh_token":"...","id_token":"...","email":"a@yourdomain.com","type":"codex"}
{"access_token":"...","refresh_token":"...","id_token":"...","email":"b@yourdomain.com","type":"codex"}
```

注意：
- 不要把 Token 填到 API Key 输入框
- 必须走 OpenAI OAuth / Codex Session Import
- `refresh_token` 很重要，缺少它后续无法自动刷新

## 14. 常见问题

### 14.1 打开就是上一个账号登录态

确认配置：

```json
{ "browserClearChatGptSession": true }
```

如果还是不行，关闭所有 Chrome 自动化窗口后删除 `browser-profile/` 再重新运行。

### 14.2 Cloudflare Worker 返回 unauthorized

检查 `mailAdminToken` 必须和 Worker 环境变量 `MAIL_API_TOKEN` 完全一致。

### 14.3 收不到邮箱验证码

按顺序检查：
1. Cloudflare Email Routing 是否启用
2. MX 记录是否正确
3. Catch-all 是否设置为 Send to Worker
4. Worker 是否绑定 D1（绑定名必须是 `DB`）
5. Worker 变量 `MAIL_DOMAIN` 是否是你的域名
6. `/api/health` 是否返回 `{"ok":true}`

### 14.4 接码平台号码国家不对

固定国家：

```json
{
  "heroSmsCountry": 46,
  "phoneCountryCode": "SE",
  "heroSmsPromptCountrySelection": false
}
```

手动选择低价国家：

```json
{ "heroSmsPromptCountrySelection": true }
```

### 14.5 接码平台切换后不生效

确认 `smsProvider` 字段取值：`herosms` / `grizzly` / `nexsms`，且对应平台的 API Key 已填写。

### 14.6 Outlook 邮箱池提示 refresh_token 为占位符

卡密中的 refresh_token 必须是真实凭据（通常 800 字符以上）。请重新获取完整卡密后导入。

### 14.7 npm test 失败

当前项目没有测试脚本，`npm test` 本来就会报错。检查语法用：

```bash
node --check index.js
node --check src/browserService.js
node --check src/config.js
node --check src/mailProvider.js
node --check src/smsProvider.js
```

## 15. 不要提交的文件

这些文件包含账号、Token、日志或浏览器登录态：

```text
accounts.json
username.json
shibai.json
token-status.json
outlook-accounts.json
tokens/
logs/
browser-profile*/
node_modules/
desktop-release/
config.json
config.local.json
config.server.json
```

对外发包建议使用 Git 提交，`.gitignore` 已配置忽略上述文件。
